**Dead Man's Switch**

A plugin that requires periodic check-ins via the Unraid web UI. If you fail to check in within a configurable time period, the plugin executes automated file deletion, custom scripts, and Docker container actions. Features Discord webhook notifications, Uptime Kuma monitoring, grace periods, dry run mode, and more.
