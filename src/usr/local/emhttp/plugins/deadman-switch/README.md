**Dead Man's Switch**

A plugin that requires periodic check-ins via the Unraid web UI. If you fail to check in within a configurable time period, the plugin executes automated file deletion, custom scripts, and Docker container actions. Features webhook notifications, trusted contacts, grace periods, dry run mode, and more.
